---
deployment:
  tasks:
    - export DEPLOYPATH=/home/tradlick/public_html
    - rm -rf $DEPLOYPATH/* && rm -rf $DEPLOYPATH/framework/* && cp -r ./ $DEPLOYPATH/framework && cp -r $DEPLOYPATH/framework/public/* $DEPLOYPATH/
    - cd $DEPLOYPATH && composer install #
    # && composer update && npm install && npm update && npm run prod
